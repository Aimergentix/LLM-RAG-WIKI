# Spark app environment template. Copy to `.env.local` (gitignored) and fill.
# The token MUST match the SPARK_BRIDGE_TOKEN exported when running uvicorn.

VITE_BRIDGE_URL=http://127.0.0.1:8765
VITE_BRIDGE_TOKEN=replace-me-with-the-bridge-token
